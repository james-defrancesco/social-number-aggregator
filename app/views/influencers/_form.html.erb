<%= form_with(model: influencer, local: true) do |form| %>

  <% if influencer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(influencer.errors.count, "error") %> prohibited this influencer from being saved:</h2>

      <ul>
      <% influencer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field is-horizontal ">
      <div class="field">
        <div class="control has-icons-left has-icons-right">
          <%= form.text_field :first_name, id: :first_name, class: "input",placeholder: "First Name" %>
          <span class="icon is-left">
            <i class="fa fa-address-card"></i>
          </span>
        </div>
      </div>

      <div class="field is-horizontal ">
        <div class="field">
          <div class="control has-icons-left has-icons-right">
            <%= form.text_field :last_name, id: :last_name, class: "input",placeholder: "Last Name" %>
            <span class="icon is-left">
              <i class="fa fa-address-card"></i>
            </span>
          </div>
        </div>
      </div>


    <div class="field">
      <div class="control has-icons-left has-icons-right">
        <%= form.text_field :instagram, id: :instagram, class: "input", placeholder: "instagram" %>
          <span class="icon is-left">
        <i class="fa fa-instagram"></i>
      </span>
      </div>
    </div>

    <div class="field">
      <div class="control has-icons-left has-icons-right">
        <%= form.text_field :twitter, id: :twitter, class: "input", placeholder: "twitter" %>
          <span class="icon is-left">
        <i class="fa fa-twitter"></i>
      </span>
      </div>
    </div>

    <div class="field">
      <div class="field-body">
        <div class="control has-icons-left has-icons-right">
          <%= form.text_field :youtube, id: :youtube, class: "input", placeholder: "youtube" %>
          <span class="icon is-left">
            <i class="fa fa-youtube"></i>
          </span>
        </div>
      </div>
    </div>

    <div class="field">
      <div class="control has-icons-left has-icons-right">
        <%= form.text_field :company, id: :company, class: "input", placeholder: "company" %>
          <span class="icon is-left">
        <i class="fa fa-company"></i>
      </span>
      </div>
    </div>

    <div class="actions">
      <%= form.submit class: "button is-primary" %>
    </div>
  </div>

<% end %>
